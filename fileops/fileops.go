package fileops

func IncludesIn(elements []int, lookingFor int) bool { // Checks if lookingFor element is in the array/slice. Returning true/false

	for i := 0; i < len(elements); i++ {

	}

	return false
}
